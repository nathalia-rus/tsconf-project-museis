declare module 'types' {
    export type MenuItem = {
        name: string
        path: string
        id: ONavigationItem
    }

    export type FormItem = {
        id: any
        value: string
    }
}
